# dagger-winget-release

## Create the command and invoke the command:

## Print the command to the console:

```powershell
dagger call create-winget-command
```

## wingetcreate 

This dagger command is being used here [https://github.com/pjmagee/dagger-winget-release](https://github.com/pjmagee/dagger-winget-release)
